import Carta from './assets/carta';
import Hero from './hero';

const MainContent = ({ name, altName, menu, theme }) => {
	return (
		<>
			<h1>Hola Mundo</h1>
		</>
	);
};

export default MainContent;
